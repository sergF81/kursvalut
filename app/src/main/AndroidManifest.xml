<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kursvalut">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bank_icon"
        android:supportsRtl="true"
        android:theme="@style/Currency">


        <activity

            android:name="com.kursvalut.InfoActivity"
            android:exported="false" />

        <activity android:theme="@style/SplashLauncher"
            android:name="com.kursvalut.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>